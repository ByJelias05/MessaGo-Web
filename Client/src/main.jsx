import React from 'react'
import ReactDOM from 'react-dom/client'
import {RoutesApp} from "./Router/RouteApp"


ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RoutesApp />
  </React.StrictMode>
)
